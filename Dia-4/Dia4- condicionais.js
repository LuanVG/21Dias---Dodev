// Condições IF E ELSE
// == Igual a, === Igual a e do mesmo tipo, > Maior que, < Menor que, >= Maior ou igual a, <= Menor ou igual a, != Diferente de, !== Diferente de e de outro tipo, && E, || OU, ! NÃO, ? : Operador ternário, switch case.


let condicao = true; // Condição verdadeira
if (condicao) { // Se a condição for verdadeira
    console.log('Verdadeiro'); // Executa esse bloco de código
} else { // Se a condição for falsa
    console.log('Falso'); 
}